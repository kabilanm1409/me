{
  "name": "portfolio-backend",
  "version": "1.0.0",
  "description": "Contact-form backend for Kabilan M's portfolio",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "deploy": "firebase deploy",
    "deploy:hosting": "firebase deploy --only hosting",
    "deploy:rules": "firebase deploy --only database,firestore"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "express-rate-limit": "^7.4.0",
    "firebase": "^12.18.0",
    "helmet": "^7.1.0",
    "nodemailer": "^6.9.14"
  }
}
